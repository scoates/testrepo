#!/bin/bash
set -x

if [[ "$CODEBUILD_BUILD_SUCCEEDING" == "1" ]]; then
    $CODEBUILD_SRC_DIR/system/ircmsg Build for branch $CODEBUILD_GIT_BRANCH succeeded.
else
    $CODEBUILD_SRC_DIR/system/ircmsg Build for branch $CODEBUILD_GIT_BRANCH failed.
fi
